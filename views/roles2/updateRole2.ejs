<html>
<head>
    <title>update role</title>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js">  </script>

    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="/public/iconselect.js-1.0/css/lib/control/iconselect.css" >
    <script type="text/javascript" src="/public/iconselect.js-1.0/lib/control/iconselect.js"></script>
    <script type="text/javascript" src="/public/iconselect.js-1.0/lib/iscroll.js"></script>

    <script>
        var iconSelect;
        var selectedText;
        var savedIcon;

        window.onload = function(){
            savedIcon = document.getElementById('savedIcon').value;
            selectedText = document.getElementById('selected-text');
            savedIcon = savedIcon-1;
            console.log('savedIcon = ',savedIcon)

            document.getElementById('my-icon-select').addEventListener('changed', function(e){
                selectedText.value = iconSelect.getSelectedValue();
            });

            iconSelect = new IconSelect("my-icon-select",
                {'selectedIconWidth':23,
                    'selectedIconHeight':23,
                    'selectedBoxPadding':1,
                    'iconsWidth':48,
                    'iconsHeight':48,
                    'boxIconSpace':1,
                    'vectoralIconNumber':6,
                    'horizontalIconNumber':6});

            var icons = [];
            icons.push({'iconFilePath':'/public/icons/1.png', 'iconValue':'1'});
            icons.push({'iconFilePath':'/public/icons/2.png', 'iconValue':'2'});
            icons.push({'iconFilePath':'/public/icons/3.png', 'iconValue':'3'});
            icons.push({'iconFilePath':'/public/icons/4.png', 'iconValue':'4'});
            icons.push({'iconFilePath':'/public/icons/5.png', 'iconValue':'5'});
            icons.push({'iconFilePath':'/public/icons/6.png', 'iconValue':'6'});
            icons.push({'iconFilePath':'/public/icons/7.png', 'iconValue':'7'});
            icons.push({'iconFilePath':'/public/icons/8.png', 'iconValue':'8'});
            icons.push({'iconFilePath':'/public/icons/9.png', 'iconValue':'9'});
            icons.push({'iconFilePath':'/public/icons/10.png', 'iconValue':'10'});
            icons.push({'iconFilePath':'/public/icons/11.png', 'iconValue':'11'});
            icons.push({'iconFilePath':'/public/icons/12.png', 'iconValue':'12'});
            icons.push({'iconFilePath':'/public/icons/13.png', 'iconValue':'13'});
            icons.push({'iconFilePath':'/public/icons/14.png', 'iconValue':'14'});
            icons.push({'iconFilePath':'/public/icons/15.png', 'iconValue':'15'});
            icons.push({'iconFilePath':'/public/icons/16.png', 'iconValue':'16'});
            icons.push({'iconFilePath':'/public/icons/17.png', 'iconValue':'17'});

            iconSelect.refresh(icons, savedIcon);
        };
    </script>

    <script>
        $(document).ready(function(){
            var oldSortID = $("#oldSortID").val();
            var select_sort = '';
            var sortArray = $("#inUseSortID").val().split(",").map(Number);
            for (var a=1;a<=99;a++){
                if(sortArray.indexOf(a) == -1 || a == oldSortID){
                    if(a == oldSortID){
                        select_sort += '<option val=' + a + ' selected="selected">' + a + '</option>';
                    }else{
                        select_sort += '<option val=' + a + '>' + a + '</option>';
                    }
                }
            }
            $('#sort_select').html(select_sort);

            var oldRoleID = $("#oldRoleID").val();
            var select = '';
            var myArray = $("#inUseRoleID").val().split(",").map(Number);
            for (i=1;i<=99;i++){
                if(myArray.indexOf(i) == -1 || i == oldRoleID){
                    if(i == oldRoleID){
                        select += '<option val=' + i + ' selected="selected">' + i + '</option>';
                    }else{
                        select += '<option val=' + i + '>' + i + '</option>';
                    }
                }
            }
            $('#some_select').html(select);



            var role1,oldRoleName,oldRoleID,roleID,roleName,sortID,icon;
            $("#<%= roles2.id %>").click(function(){
                role1=$(this).attr('id');
                oldRoleID=$("#oldRoleID").val();
                roleID=$("#some_select").val();
                oldRoleName=$("#oldRoleName").val();
                roleName=$("#roleName").val();
                sortID=$("#sort_select").val();
                icon=$("#selected-text").val();

                //-----forms validation------------------------------------
                if (roleName == "" ) {
                    alert("ROLE NAME must be filled out");
                    return false;
                }
                if (roleID == "" ) {
                    alert("ROLE ID must be filled out");
                    return false;
                }
                //--------------------------------forms validation end-------

                $.post("/roles2/updateRole2",{
                    roleToUpdate: role1,
                    oldRoleID: oldRoleID,
                    roleID: roleID,
                    oldRoleName: oldRoleName,
                    roleName: roleName,
                    sortID: sortID,
                    icon: icon

                },function(data){
                    if(data==='done')
                    {
                        alert("login success");
                    }
                    window.location = data.redirect
                }).error(function(error){
                    if(error.responseText == 'showAlert')
                        alert("Role Name already already in use. Choose a different Name.")

                });
            });
        });
    </script>
</head>
<body>
<h1><%= title %></h1>
<form action="/dashboard">
    <input type="submit" value="Home"><br>
</form>
<form>

    <label for="sortID"><b>Sort ID:</b></label><br>
    <input type="hidden" id="oldSortID" value="<%= roles2.sortID %>">
    <input type="hidden" id="inUseSortID" value="<%= arraySort %>">

    <select id='sort_select'></select><br><br>

    <label for="roleName"><b>Role Name:</b></label><br>
    <input type="text" id="roleName" size="40" value="<%= roles2.roleName %>"><br><br>


    <label for="icon"><b>Icon:</b></label><br>
    <div id="my-icon-select"></div>
    <input type="text" id="selected-text" name="selected-text" style="width:65px;" hidden>
    <input type="text" id="savedIcon" name="savedIcon" value="<%= roles2.icon %>" hidden>


    <input type="hidden" id="inUseRoleID" value="<%= array %>">

    <% if (userAuthID == 1) {%>
    <label for="roleID"><b>Role ID:</b></label><br>
    <select id='some_select'></select><br><br>
    <% } else { %>
    <select id='some_select' style="display:none;"></select>
    <% } %>


    <input type="hidden" id="oldRoleName" value="<%= roles2.roleName %>">
    <input type="hidden" id="oldRoleID" value="<%= roles2.roleID %>"><br>

    <% if (aclModifyRoles2 == null || aclModifyRoles2.checkBoxValue == true) {%>
    <input type="button" id="<%= roles2.id %>" value="Submit" ><br>
    <% } else { %>
    <input type="button" id="<%= roles2.id %>" value="Submit" disabled><br>
    <% } %>

</form>
<form action="/roles2/showRoles2">
    <% if (aclModifyRoles2 == null || aclModifyRoles2.checkBoxValue == true) {%>
    <input type="submit" value="Back" ><br>
    <% } else { %>
    <input type="submit" value="Back" disabled><br>
    <% } %>

</form>
</body>
</html>